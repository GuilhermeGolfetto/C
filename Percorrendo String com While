#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main (){
    char palavra[20];
    printf("Palavra:");
    gets(palavra);
    int i = 0;
    while(palavra[i] != '\0'){
        printf("%c \n", palavra[i]);
        i++;
    }
    system("PAUSE");
    return 0;
}
